#include "main.h"
#include <stdio.h>

/**
* _isdigit - function that checks for a digit (0 through 9).
* @c: check character is digit.
* Return: 1 if c is a digit.
*/

int _isdigit(int c)
{
	return (c  >= '0' && c <= '9');
}

