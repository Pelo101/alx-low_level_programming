#include "main.h"

/**
 * _isdigit - function checks whether character is a digit
 * @c: is the integer that is checked
 * Return: 1 (success) or 0 (not success)
 */
int _isdigit(int c)
{

	if (c  >=  57  &&  c  <= 57 )
	{
		return (1);
	}

	return (0);
}
